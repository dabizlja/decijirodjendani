<!-- Sign In Modal -->
<% sign_in_resource = @sign_in_user || (defined?(resource) ? resource : nil) || User.new %>
<% sign_in_modal_visible = @show_sign_in_modal || sign_in_resource.errors.any? %>
<div data-modal
     id="sign-in-modal"
     class="<%= sign_in_modal_visible ? "flex" : "hidden" %> fixed inset-0 z-50 items-center justify-center px-4 py-8 sm:px-6"
     aria-hidden="<%= sign_in_modal_visible ? "false" : "true" %>"
     role="dialog"
     aria-modal="true"
     aria-labelledby="sign-in-title">
  <div data-modal-overlay class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm transition-opacity"></div>
  <div class="relative w-full max-w-md">
    <div class="overflow-hidden rounded-3xl bg-white shadow-2xl ring-1 ring-black/5">
      <div class="flex justify-end p-4">
        <button type="button"
                data-modal-close="sign-in-modal"
                class="rounded-full bg-purple-50 p-2 text-gray-500 transition hover:bg-purple-100 hover:text-gray-700 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:ring-offset-2"
                aria-label="Zatvori modal">
          <svg class="h-4 w-4" viewBox="0 0 20 20" fill="none" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 6l8 8M6 14L14 6"/>
          </svg>
        </button>
      </div>

      <div class="px-8 pb-10">
        <div class="text-center">
          <div class="mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full bg-gradient-to-br from-blue-500 to-purple-500 text-2xl text-white shadow-lg">
            🔑
          </div>
          <h3 id="sign-in-title" class="text-2xl font-bold text-gray-900">Prijavi se</h3>
          <p class="mt-3 text-sm text-gray-600">
            Pristupi svom nalogu da upravljaš svojim biznisom i vidis rezervacije.
          </p>
        </div>

        <div class="mt-6">
          <%= button_to user_google_oauth2_omniauth_authorize_path,
                         method: :post,
                         class: "w-full rounded-2xl border border-gray-200 bg-white px-4 py-3 text-sm font-medium text-gray-700 shadow-sm transition hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:ring-offset-2 flex items-center justify-center gap-2" do %>
            <svg class="h-4 w-4" viewBox="0 0 24 24">
              <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
              <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
              <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
              <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
            </svg>
            Prijavi se sa Google
          <% end %>
        </div>

        <div class="flex items-center py-6">
          <div class="flex-1 border-t border-gray-200"></div>
          <span class="mx-3 text-xs font-semibold uppercase tracking-wide text-gray-400">ili</span>
          <div class="flex-1 border-t border-gray-200"></div>
        </div>

        <% if sign_in_resource.errors.any? %>
          <div class="mb-4 rounded-2xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">
            <p class="font-semibold mb-1">Nismo mogli da te prijavimo:</p>
            <ul class="list-disc list-inside space-y-1 text-xs sm:text-sm">
              <% sign_in_resource.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <%= form_with model: sign_in_resource,
                      scope: :user,
                      url: user_session_path,
                      method: :post,
                      local: true,
                      class: "space-y-5" do |f| %>
          <!-- Email -->
          <div>
            <%= f.label :email, "Email adresa", class: "block mb-2 text-sm font-semibold text-gray-800" %>
            <%= f.email_field :email,
                              required: true,
                              autocomplete: "email",
                              class: "w-full rounded-2xl border-2 border-gray-200 bg-white px-4 py-3 text-sm text-gray-900 transition-all duration-200 placeholder-gray-400 focus:border-transparent focus:outline-none focus:ring-2 focus:ring-purple-500",
                              placeholder: "tvoj@email.com",
                              data: { modal_focus: true } %>
          </div>

          <!-- Password -->
          <div>
            <%= f.label :password, "Lozinka", class: "block mb-2 text-sm font-semibold text-gray-800" %>
            <%= f.password_field :password,
                                  required: true,
                                  autocomplete: "current-password",
                                  class: "w-full rounded-2xl border-2 border-gray-200 bg-white px-4 py-3 text-sm text-gray-900 transition-all duration-200 placeholder-gray-400 focus:border-transparent focus:outline-none focus:ring-2 focus:ring-purple-500",
                                  placeholder: "Unesite lozinku" %>
          </div>

          <!-- Remember me and forgot password -->
          <div class="flex items-center justify-between pt-2">
            <div class="flex items-center gap-2">
              <%= f.check_box :remember_me,
                              class: "h-4 w-4 rounded border-2 border-gray-300 text-purple-600 focus:ring-2 focus:ring-purple-500" %>
              <%= f.label :remember_me, "Zapamti me", class: "text-xs text-gray-600" %>
            </div>
            <a href="#" class="text-xs text-purple-600 hover:text-purple-700 font-medium">Zaboravili ste lozinku?</a>
          </div>

          <!-- Submit button -->
          <%= f.submit "🔑 Prijavi se",
                       class: "w-full rounded-2xl bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold py-3 shadow-lg transition hover:brightness-110" %>
        <% end %>

        <!-- Switch to registration -->
        <div class="mt-6 text-center">
          <p class="text-sm text-gray-600">
            Nemate nalog?
            <button type="button"
                    data-modal-close="sign-in-modal"
                    data-modal-target="business-registration-modal"
                    data-modal-toggle="business-registration-modal"
                    class="text-purple-600 hover:text-purple-700 font-medium">
              Registrujte se ovde
            </button>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>
