<%= render "dashboard/dashboard/header",
    title: "Uredite Biznis ✏️",
    subtitle: "Ažurirajte informacije o #{@business.display_name}",
    content_width: "max-w-4xl" %>

<!-- Main Content -->
<div class="max-w-4xl mx-auto px-6 py-4">
  <!-- Form -->
  <div class="bg-white rounded-2xl shadow-lg border border-gray-100 p-8">
    <%= render 'form', business: @business %>
  </div>

  <!-- Danger Zone -->
  <div class="mt-8 bg-red-50 border border-red-200 rounded-2xl p-6">
    <h3 class="font-semibold text-red-800 flex items-center gap-2 mb-4">
      <span class="text-xl">⚠️</span>
      Zona opasnosti
    </h3>
    <p class="text-sm text-red-700 mb-4">
      Brisanje biznisa je nepovratna akcija. Svi podaci će biti obrisani.
    </p>
    <%= link_to "🗑️ Obriši biznis",
        dashboard_business_path(@business),
        method: :delete,
        class: "inline-block px-6 py-2 bg-red-600 text-white rounded-lg font-medium hover:bg-red-700 transition-colors",
        confirm: "Da li ste sigurni da želite da obrišete '#{@business.display_name}'? Ova akcija se ne može poništiti." %>
  </div>
</div>
